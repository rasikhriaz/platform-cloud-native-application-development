apiVersion: v1
kind: Service
metadata:
  name: platform-demo
  namespace: platform-demo
spec:
  type: NodePort
  selector:
    app: platform-demo
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
